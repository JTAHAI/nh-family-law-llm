[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nh-family-law-llm"
version = "8.0.10"
description = "Local-first New Hampshire family-law research, evidence, drafting, and optional loopback local-model workbench with exact context approval and provenance receipts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pypdf>=6.16.1,<7",
  "pypdfium2>=5.12.1,<6",
  "python-docx>=1.2.0,<2",
  "defusedxml>=0.7.1,<1",
  "docx-editor>=0.7.1,<0.8",
  "pywin32>=311; sys_platform == 'win32'",
  "cryptography>=50,<51",
  "h2>=4.4.1,<5",
]

[project.scripts]
nhfl = "nh_family_law_llm.cli:main"
nhfl-fast-interchange-worker = "legal.fast_interchange.worker:main"

[project.optional-dependencies]
api = [
  "fastapi==0.139.2",
  "starlette==1.3.1",
  "uvicorn==0.51.0",
  "httpx>=0.28.1,<1",
  "python-multipart>=0.0.20,<1",
]
dev = ["pytest>=9,<10", "ruff>=0.14.14,<1", "httpx>=0.28.1,<1", "pip-audit==2.10.1"]
document-intelligence = [
  "docling>=2.114.0,<3",
  "presidio-analyzer>=2.2.363,<3",
  "ocrmypdf>=17.8.1,<18",
  "spacy>=3.8.0,<4",
  "pikepdf>=10.0.0,<11",
  "fpdf2>=2.8.0,<3",
  "uharfbuzz>=0.41.0,<1",
]
fast-interchange = [
  "fastapi==0.139.2",
  "uvicorn==0.51.0",
  "torch>=2.5,<3",
  "transformers>=4.46,<6",
  "peft>=0.14,<1",
  "safetensors>=0.4,<1",
  "psutil>=6,<8",
]
retrieval-evaluation = ["sqlite-vec>=0.1.9,<0.2", "qdrant-client>=1.18.0,<2", "deepeval>=4.0.5,<5"]
release-hardening = [
  "opentelemetry-api>=1.42.1,<2",
  "opentelemetry-sdk>=1.42.1,<2",
  "Pillow>=12.3.0,<13",
]

[tool.setuptools.package-dir]
nh_family_law_llm = "src/nh_family_law_llm"

[tool.setuptools.packages.find]
where = [".", "src"]
include = ["app*", "legal*", "nh_family_law_llm*"]

[tool.setuptools.package-data]
nh_family_law_llm = [
  "ui/*.html", "ui/*.css", "ui/*.js", "ui/*.svg",
  "ui/brand/*.png", "ui/brand/*.svg",
  "resources/brand-kit/css/*.css",
  "resources/runtime/configs/*.json",
  "resources/brand-kit/assets/favicon/*",
  "resources/brand-kit/assets/logo/*",
  "data/authority_snapshot/manifest/*.json",
  "data/authority_snapshot/CURRENT_AUTHORITY/official_guidance/*.md",
  "data/authority_snapshot/CURRENT_AUTHORITY/statutes/*.md",
  "data/authority_snapshot/CURRENT_AUTHORITY/statutes/*/*.md",
  "data/authority_snapshot/PENDING_REVIEW/FUTURE_EFFECTIVE/*.md",
  "data/authority_snapshot/*.md",
  "data/evals/*.json",
  "data/evals/*.jsonl",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "src"]
addopts = "-q"
filterwarnings = [
  "ignore::DeprecationWarning"
]

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
  "runtime",
  "uploads",
  "vectorstores",
  "corpora",
  "node_modules",
  "dist",
  ".venv",
  "*.egg-info",
  ".eggs"
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
