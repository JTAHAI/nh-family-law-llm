PyInstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
fastapi==0.139.2
starlette==1.3.1
uvicorn==0.51.0
# Required by the canonical production API's multipart record-import routes.
# FastAPI checks for it lazily when it registers Form/File dependencies, so it
# must be an explicit Store-runtime dependency rather than a development extra.
python-multipart>=0.0.20,<0.1
httpx==0.28.1
pypdf>=6.16.1,<7
pypdfium2==5.12.1
cryptography>=50,<51
python-docx>=1.2.0,<2
docx-editor>=0.7.1,<0.8
h2>=4.4.1,<5
Pillow==12.3.0
# The full offline tier ships the optional FAST INTERCHANGE runtime, but never
# legal weights or adapters.  PEFT and its execution helper are required to
# activate a separately admitted external LoRA pack without a runtime install.
peft>=0.14,<1
psutil>=6,<8
accelerate>=1.0,<2
safetensors>=0.4,<1
docling>=2.114.0,<3
presidio-analyzer>=2.2.363,<3
ocrmypdf>=17.8.1,<18
spacy>=3.8.0,<4
sqlite-vec>=0.1.9,<0.2
qdrant-client>=1.18.0,<2
pikepdf>=10.0.0,<11
fpdf2>=2.8.0,<3
uharfbuzz>=0.41.0,<1
