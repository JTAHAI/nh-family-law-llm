# Release blockers

- Official authority coverage/currentness is not complete. The external acquisition
  at `D:\NHFamilyLawLLM-authority-data\run-20260912-full-catalog-repaired-r2`
  preserves 33 original official responses and hashes from the 37-target seed
  catalog. Four retired Judicial Branch rule targets still return HTTP 404;
  those sources are neither substituted nor promoted.
- Independent New Hampshire attorney review and sign-off have not been obtained.
  The raw acquisition is not a legal review, a proposition-verification record,
  or authority-currentness certification.
- The 2026-09-13 pending amendment is fail-closed. On and after its effective
  date the old RSA 461-A:6 capsule is blocked until a fresh official source has
  been reviewed and explicitly promoted.
- A clean declared-dependency installation, source- and installed-wheel browser
  acceptance, and frozen executable smoke check have passed. A fresh unsigned
  MSIX at `dist/store-submission-v8.0.10-prelaunch-r3/msix/` has passed static
  manifest, archive, payload, path, bundled-engine, and private-data audits;
  its hash is recorded in the prelaunch execution log. Full Windows test-suite
  reconciliation, installed-package qualification, WACK, accessibility evidence,
  signed-package installation, and clean-machine evidence remain incomplete.
- The reserved Partner Center identity is configured for `TAHAI Web Services`,
  but `production_identity_confirmed` is false. No approved production signing
  certificate, completed Store submission, or Microsoft certification was
  supplied or claimed.

The restored evaluation fixtures are newly authored synthetic engineering evaluations. They do not establish legal accuracy, exhaustive coverage, or attorney approval.
