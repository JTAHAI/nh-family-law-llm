# v8 source checkpoint and Terra handoff

## Checkpoint scope

This checkpoint preserves the coherent v8 source work completed before the next
Terra build campaign. It is a source checkpoint, not Microsoft Store, attorney,
pilot, or Enterprise-GA evidence.

- Product version: `8.0.0`
- Package target: `8.0.0.0`
- Branch at checkpoint: `main`
- Completed roadmap slices: 26 of 200
- Explicitly blocked slice: 23 (no admitted local cross-encoder/reranker model)
- Next implementation slice: 28, pinpoint resolver expansion
- Remaining planned slices: 174, including the blocked slice 23 admission work

The detailed queue and per-slice truth status remain in
[`NEXT_200_UPGRADES.md`](NEXT_200_UPGRADES.md).

## Preserved implementation

- v8 chat-first workbench with collapsible supporting surfaces, closed-by-default
  evidence drawer, stronger contrast, responsive expansion, and multiple views.
- Streaming and cancellable source-backed chat with correction, comparison,
  branching, response depth, audience presentation, clarification, assumption,
  fact-pin, decomposition, contradiction, usefulness, timing, and source-state
  review paths.
- Hybrid local retrieval, guarded New Hampshire query expansion, citation-graph neighbors,
  temporal review, negative-treatment status, and authority-conflict explanation.
- Matter-scoped Add-on Studio and Productivity Studio services, canonical APIs,
  encrypted local state, append-only receipts, role checks, source/artifact
  drill-down, review-required status, and production UI actions.
- Native Whisper provisioning contract and retained MIT notice. Execution remains
  fail-closed until an admitted binary and hash are provisioned.
- v8 version, release-scope, packaging, documentation, attribution, and Store
  metadata updates.

## Cleanup repairs made before checkpoint

- Restored the non-technical source-launch instructions and Store download link.
- Restored safe local startup behavior: port validation, environment reuse,
  explicit dependency refresh/reset, and verified service shutdown.
- Refused unverified PID termination and reparse-point workspace deletion.
- Restored fixed public-safe Promptfoo and Semgrep policies.
- Aligned the default closed drawer with its `aria-expanded` and `aria-hidden`
  state.
- Replaced stale 6.0.4 shipment-test versions with the canonical product version.
- Updated canonical release notes and pass log for v8 while preserving the
  completed source-foundation pass marker.
- Hardened the attribution generator so tests retain permissive-dependency
  credits and current repository/version metadata.

## Verification snapshot

- Python compilation: pass.
- Production JavaScript syntax: pass for both shipped mirrors.
- PowerShell parse: pass for launch, stop, repair, Whisper provisioning, and
  Store runtime build scripts.
- Test collection: 1,234 tests.
- Canonical route inventory: 246 method/path pairs, zero duplicates.
- Source/package API, UI, CSS, HTML, and version mirrors: byte-identical.
- Final changed-surface battery: 80 passed, 1 skipped in 165.43 seconds.
- Optional-engine skip: native Whisper execution; no admitted binary is present
  in this source checkout.

The repository was also exercised in bounded full-suite groups. All discovered
source defects were repaired and their focused regressions pass. Nine checks
remain dependent on external or historical artifacts that are not present in
this checkout:

1. Three v7 authority-acceptance checks require
   `dist/release/v7.0.0/evidence/04_authority_acceptance.json` and
   `04_retrieval_verifier_metrics.json`.
2. Three fictional GA end-to-end evidence checks require the prior
   `dist/ga_today` matrix, manifest, and PNG evidence.
3. Two Store-preflight checks require the exact
   `dist/store/evidence/bundled-engine-inventory.json` package artifact.
4. One installed-runtime check currently finds an older installed executable
   that reports health as `ok` instead of the current `ready`/`degraded`
   vocabulary.

These artifacts were not fabricated. The ignored `dist` archive was preserved
because it contains historical packages and evidence that may still be useful.

## Release truth and next action

This checkpoint is ready for continued source development. It does not establish
frozen-runtime reachability for roadmap slices 1–27, a qualified v8 MSIX,
attorney-reviewed evaluation, a controlled pilot, or Enterprise GA.

Resume with slice 28. Keep slice 23 blocked until a local reranker model,
license, hash, admission record, package boundary, and measured evaluation are
available. Continue to use the universal vertical definition of done in the
roadmap before advertising any additional slice.
