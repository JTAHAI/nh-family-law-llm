# Evidence Review r0011: bounded production-source E2E verification

## Current disposition — 2026-09-03

This document retains the 2026-09-01 historical run, but it must **not** be
read as a current operational acceptance. A repeat with the current worktree
and real r0011 weights found a 120-second CPU inference timeout. A separate
offline RTX 3060 diagnostic then generated real weight output at 64 and 256
tokens, but neither result stopped naturally or reproduced both exact fictional
source values. The host correctly withheld raw prose and returned its
review-required fallback.

Therefore r0011 is a failed, unadmitted fictional research candidate—not a
user-operable specialist—and is hidden from the production registry and MSIX.
The retained JSON evidence contains only fictional source metadata, hashes,
timings, and safety verdicts; the owned test worker shut down cleanly with no
snapshot remaining. A fresh corrective candidate must pass exact-source,
natural-completion, and held-out regression gates before another UI E2E claim.

Recorded 2026-09-01. The exact pack
`nhfl-evidence-review-research-evidence-r0011-final` is operational as a
**verifier-mediated, fictional-data research specialist**. It is not an
admitted legal model and must not be used as a filing-ready or attorney-reviewed
answer source.

## Candidate and training identity

- Shared base: Qwen3-0.6B, Apache-2.0, 1,503,300,328 bytes.
- Evidence Review adapter: 40,422,168 bytes; SHA-256
  `75a57a5025cbf424fcdfd6b50efdfc572cff79202d9823ec7fb78a452d43399f`.
- Training: 25,600 fictional examples, 3,200 optimizer steps, and 2,477,262
  supervised tokens in 8,546.172 seconds.
- The adapter parameter digest changed from
  `dd8d7ae3c35b79b1ec31642d96685f8929e9058aef7d1515c7bcbd522a839aa2`
  to
  `e5789513f26b1ed3a9e1d5ffa8b1dac98a607f6e99fec00037a099a9fad950eb`.
- Training used BF16 LoRA rank 16 on an RTX 3060. Peak allocated GPU memory was
  3,472.88 MiB. This is a development observation, not a low-end-hardware
  qualification.
- The self-contained research pack is 1,559,648,425 bytes and exists once at
  `dist/model-candidates/evidence-r0011/evidence-r0011-final`.

The training authorization permits local, fictional research only. It does not
provide production admission, attorney approval, or authority to represent the
adapter as substantive New Hampshire-law expertise.

## What passed

- Real-weight GPU inference covered one held-out example from each of 29 failure
  families in the development, test, and challenge splits: **87/87 mechanical
  checks passed**. Exact target matches were 27/29, 28/29, and 27/29.
- The production host now discards raw model narrative and reconstructs the
  visible answer only from exact, source-bound record excerpts.
- A valid excerpt may remain visible when a separate proposed quote is inexact
  or contains a strongly labeled private identifier. The rejected material is
  suppressed, the precise blocker is shown, and the result stays review-required.
- Source cards now carry the verifier's exact character offsets into the real
  source-preview action rather than opening an unbounded document preview.
- Focused output-boundary, worker, source-binding, completion-boundary, API, and
  production-UI tests passed: **108 passed**. Production JavaScript syntax
  validation also passed.

## Production-source app journey

The actual production-source desktop workbench and canonical local API opened a
clearly fictional matter with two conflicting pickup-time records. The user
selected those private records and explicitly approved the hash-bound context.

- The real Qwen3-0.6B plus r0011 adapter loaded on CPU through the authenticated,
  loopback-only FAST INTERCHANGE worker.
- The cold request completed with HTTP 200 in 79.267 seconds. A warm repeat
  completed with HTTP 200 in 19.950 seconds.
- Both requests returned `completed_review_required`, two exact verified spans,
  and no falsely established fact or legal conclusion.
- The visible response contained only the two verified record sentences and a
  deterministic warning that model narrative was withheld.
- The source-card action opened the exact sentence
  `The pickup note says pickup at 15:20.` rather than the whole record.
- Provenance receipt SHA-256 values were
  `733b941e226d60aa5d352fce5c0b16b338a4d8d78e2c590640976a1195241f15`
  and
  `dfc9fb9a35f050f10ddeb568091f4491fe66d5ccd5b1bc7fe8c7b1570c402c79`.
- Production trust rejected the development test signer with
  `fast_interchange_test_signer_forbidden`. The signer existed only inside the
  isolated fictional E2E harness.
- Shutdown was clean. Peak worker resident memory was 4,991,594,496 bytes
  (about 4.65 GiB), retained QA evidence was about 171 KiB, and the QA output
  contained zero model snapshots.

Evidence is retained under `dist/model-candidates/evidence-r0011/` and
`dist/qa801/evidence-r0011/in-app-r1/`. Both are repository-local ignored
artifacts. No drive-root scratch workspace or repeated model copy was created.

## Quality boundary and release truth

The raw adapter is **not a reliable standalone answerer**. On the older
free-wording regression, it passed only **5/12** aggregate mechanical cases:
source-reference checks were 12/12, quote binding 10/12, meaning checks 7/12,
and forbidden-literal checks 11/12. Manual review also found that a derivative
copy explanation could reverse the direction of derivation when record order
changed. Those findings are why raw narrative is never shown.

This result proves one useful and safe product path: the adapter can propose
record excerpts, the deterministic host verifies them against approved private
records, and the UI exposes only exact verified spans with review-required
status and provenance. It does **not** establish substantive New Hampshire-law accuracy,
attorney review, production admission, frozen-app or installed-MSIX reachability,
Store readiness, Enterprise GA, or filing use.

Remaining release gates are independent human evaluation, production-signed
admission for the exact pack hashes, a stronger free-wording evaluation result,
target-PC memory and latency qualification, frozen/package verification, and a
clean full regression.
